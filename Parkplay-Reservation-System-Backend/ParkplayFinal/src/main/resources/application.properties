spring.application.name=ParkplayFinal

# --- Conexiunea la Baza de Date ---
# URL-ul standard pentru MySQL local
spring.datasource.url=jdbc:mysql://localhost:3306/parkplay_db?allowPublicKeyRetrieval=true&useSSL=false

# Userul (root este default)
spring.datasource.username=root

# Parola pe care ai setat-o la pasul anterior (ALTER USER...)
spring.datasource.password=parolaProiectDBP3

# --- Set?ri JDBC ---
# Aceast? linie spune Spring-ului s? ruleze fi?ierul schema.sql la pornire!
spring.sql.init.mode=always

# (Op?ional) Dac? vrei s? vezi logurile SQL în consol?
logging.level.org.springframework.jdbc.core.JdbcTemplate=DEBUG